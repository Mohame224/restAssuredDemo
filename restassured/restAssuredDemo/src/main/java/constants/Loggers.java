package constants;
import org.apache.logging.log4j.LogManager;

import org.apache.logging.log4j.Logger;

import listener.ReportListener;
public class Loggers {
	public static Logger logger=LogManager.getLogger(ReportListener.class);
}
